import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function SalesPage() {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Sales &amp; Point of Sale (POS)</CardTitle>
          <CardDescription>Process sales, manage invoices, and handle returns.</CardDescription>
        </CardHeader>
        <CardContent>
          <p>The Point of Sale interface and sales management features will be implemented here.</p>
        </CardContent>
      </Card>
    </div>
  );
}
